services:
  postgres:
    container_name: radar-backend-pg
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: radar-backend
      PGDATA: /data/postgres
    volumes:
      - ./data/pg:/data/postgres
